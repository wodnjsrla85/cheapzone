import 'package:flutter/material.dart';

class InsertInquiry extends StatelessWidget {
  const InsertInquiry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}